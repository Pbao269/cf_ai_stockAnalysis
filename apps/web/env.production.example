# Production Environment Configuration
# This file should be used for production deployment
# Copy this to .env.production.local for production builds

# API Gateway URL (replace with your actual deployed URL)
NEXT_PUBLIC_API_URL=https://api-gateway-mvp.your-subdomain.workers.dev

# Optional: Add other production-specific environment variables
NEXT_PUBLIC_ENVIRONMENT=production
